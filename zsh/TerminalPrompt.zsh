PROMPT='%B%F{blue}-> %n:[%~]# %f%b'

# %B means bold text. It must be used with < %b >
# %F means color of text. It must used with {color_name} and finish tag < %f >
# %n means your domain. Such as 'YourName'
# %~ means path to current terminal.

#  ], [, ->, # symbols are only string. They are not specific code.
